<!DOCTYPE html>
<html lang="zh_CN">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    * {
      margin: 0;
      padding: 0;
    }

    .email-container {
      max-width: 600px;
      margin: 20px auto;
      font-family: 'Segoe UI', system-ui, sans-serif;
    }

    .header {
      border-bottom: 2px solid #e0e0e0;
      padding-bottom: 15px;
      margin-bottom: 25px;
      display: flex;
    }

    .logo {
      height: 40px;
      border-radius: 50%;
      margin-right: 16px;
    }

    .content {
      line-height: 1.6;
      color: #333;
    }

    .result-block {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 16px;
      white-space: pre-wrap;
    }

    .footer {
      margin-top: 30px;
      color: #666;
      font-size: 0.9em;
      text-align: center;
    }
  </style>
  <link href="https://cdn.bootcdn.net/ajax/libs/highlight.js/11.11.1/styles/default.min.css" rel="stylesheet">
  <script src="https://cdn.bootcdn.net/ajax/libs/highlight.js/11.11.1/highlight.min.js"></script>
  <script>hljs.highlightAll();</script>
</head>

<body>
  <div class="email-container">
    <div class="header">
      <img src="{{ logoUrl }}" class="logo" alt="User avatar">
      <h3 style="font-size: 1.2em;line-height: 40px;">{{ appName }}</h3>
    </div>

    <div class="content">
      <p style="margin-bottom: 10px;">任务于 {{ timestamp }} 执行完成。状态：<strong>{{ status }}</strong></p>

      <div class="result-block">
        {{#if isError}}
        <div>
          <h3 style="color: #dc3545;margin-bottom: 5px;">❌ 错误详情：</h3>
          <pre><code class="language-json">{{result}}</code></pre>
        </div>
        {{else}}
        <div>
          <h3 style="color: #28a745;margin-bottom: 5px;">✅ 执行结果：</h3>
          <pre><code class="language-json">{{result}}</code></pre>
        </div>
        {{/if}}
      </div>
    </div>

    <div class="footer">
      <p>此邮件由系统自动发送&nbsp;请勿直接回复</p>
      <p>© {{ currentYear }}&nbsp;<a href="https://yuanki.cn">{{ companyName }}</a>&nbsp;保留所有权利</p>
    </div>
  </div>
</body>

</html>